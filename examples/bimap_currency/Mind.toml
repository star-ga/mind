[package]
name = "bimap_currency"
version = "0.1.0"
authors = ["STARGA Inc."]
license = "Apache-2.0"
description = "Single-source bijective map over a nice set: one declaration generates both directions"

[build]
target = "cpu"
entry = "main.mind"
