// FAIL_CLOSED (in-subset): struct-lit construction as a value if-expr branch.
// (Also fails closed in plain-body position in the whole-module nfn driver.)
struct S { a: i64, b: i64 }
pub fn f(c: i64, p: i64, q: i64) -> i64 {
    if c == 0 { mk(S { a: p, b: q }) } else { 0 } }
pub fn mk(z: i64) -> i64 { z }
