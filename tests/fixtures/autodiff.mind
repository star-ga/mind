// Minimal differentiable program for the --emit-grad-ir CLI test.
// f(x) = x*x + x  (a clean, lowerable diff-tensor expression).
fn main() {
    let x: diff tensor<f32> = 3.0;
    let y = x * x + x;
    print(y);
}
