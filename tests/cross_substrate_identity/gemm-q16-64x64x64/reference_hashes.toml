# mind-bench reference hashes — RFC 0020 §5. See dot-l2-q16/reference_hashes.toml
# for full provenance/signing notes. Q16.16 GEMM is exact integer arithmetic
# (M·N per-element dot_q16 reductions, composed via the gemv intrinsic over Bᵀ),
# so avx2 == neon by construction (RFC 0015 §3.1).
#
#   avx2 — verified on x86_64 (this gate, 2026-05-26).
#   neon — asserted identical per RFC 0015 §3.1; enforced on the aarch64 CI runner.

avx2 = "92e2cb75d74d83a4a398d78d9ac560f195279c31814972c892f856f675faea0f"
neon = "92e2cb75d74d83a4a398d78d9ac560f195279c31814972c892f856f675faea0f"
